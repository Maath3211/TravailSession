<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Travail_session.affichage_employe"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Travail_session"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <GridView x:Name="gvView" Grid.Row="1" Grid.Column="0" SelectionChanged="gvView_SelectionChanged" Margin="0,20,0,0">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local:employes">
                    <StackPanel Orientation="Vertical" Width="280">
                        <Image Source="https://randomuser.me/api/portraits/men/58.jpg" />
                        <TextBlock Text="{x:Bind Photo}" Margin="10,0,0,0" FontSize="18"/>
                        <TextBlock Text="{x:Bind Prenom}" Margin="10,0,0,0" FontSize="18"/>
                        <TextBlock Text="{x:Bind Nom }" Margin="10,0,20,50" FontSize="18"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
